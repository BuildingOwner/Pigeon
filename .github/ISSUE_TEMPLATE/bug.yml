name: "🐛 Bug Report"
description: "버그를 발견했을 때 사용"
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        ## 버그 리포트
        발견한 버그에 대해 상세히 설명해주세요.

  - type: textarea
    id: description
    attributes:
      label: 버그 설명
      description: 어떤 버그인지 설명해주세요
      placeholder: "폴더 트리가 제대로 렌더링되지 않음"
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 재현 방법
      description: 버그를 재현하는 단계
      placeholder: |
        1. 메인 페이지 접속
        2. 좌측 폴더 트리 확인
        3. 3단계 이상 깊이의 폴더가 표시되지 않음
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 예상 동작
      description: 원래 어떻게 동작해야 하는지
      placeholder: "모든 깊이의 폴더가 표시되어야 함"
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: 실제 동작
      description: 실제로 어떻게 동작하는지
      placeholder: "3단계 이상 폴더가 표시되지 않음"
    validations:
      required: true

  - type: input
    id: browser
    attributes:
      label: 브라우저
      description: 사용한 브라우저
      placeholder: "Chrome 120"
    validations:
      required: false

  - type: textarea
    id: console
    attributes:
      label: 콘솔 에러
      description: 브라우저 콘솔에 표시된 에러
      render: shell
    validations:
      required: false

  - type: textarea
    id: screenshots
    attributes:
      label: 스크린샷
      description: 버그 관련 스크린샷
    validations:
      required: false

  - type: dropdown
    id: priority
    attributes:
      label: 심각도
      options:
        - Critical - 서비스 불가
        - High - 주요 기능 장애
        - Medium - 부분 기능 장애
        - Low - 사소한 문제
    validations:
      required: true
